import PagePlaceholder from "@/components/library/PagePlaceholder";

export default function AdminEvents() {
  return (
    <PagePlaceholder
      description="Manage wedding events and the schedule. Real event management lands in a later issue."
      title="Events"
    />
  );
}
