import PagePlaceholder from "@/components/library/PagePlaceholder";

export default function AdminEmails() {
  return (
    <PagePlaceholder
      description="Compose and send emails to guests. Real email tooling lands in a later issue."
      title="Emails"
    />
  );
}
