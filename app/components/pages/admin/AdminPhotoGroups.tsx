import PagePlaceholder from "@/components/library/PagePlaceholder";

export default function AdminPhotoGroups() {
  return (
    <PagePlaceholder
      description="Manage photo groups for each event. Real photo group management lands in a later issue."
      title="Photo Groups"
    />
  );
}
