// The IANA timezone of the wedding venue. Event times are stored as the
// venue's wall-clock values (DATE + "HH:MM"), so this zone exists purely to
// label displayed times; changing it never changes stored data.
export const VENUE_TIME_ZONE = "America/Chicago";
